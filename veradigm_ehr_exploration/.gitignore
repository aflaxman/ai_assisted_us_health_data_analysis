.venv/
outputs/
.ipynb_checkpoints/
__pycache__/

# The private schema configuration: it holds the delivery's own table and field
# names, types, fill rates and permitted-value lists, which the license bars from
# any repository as a derivative schema. Committed code reads it through
# schema_config.py; config.example.json documents the structure.
config.local.json
